@AGENTS.md

Project instructions live in `AGENTS.md` (single source of truth — edit that file, not this one). This pointer keeps Claude Code and every other coding agent reading the same instructions.
